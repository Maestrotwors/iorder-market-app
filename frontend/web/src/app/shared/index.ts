export { AuthService } from './api/auth.service';
export { roleGuard } from './guards/auth.guard';
export type { BetterAuthUser, BetterAuthSession, SessionResponse, SignUpResponse, AuthError } from './types/auth.types';
